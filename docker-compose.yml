version: '3.8'
services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:latest
    container_name: sqlserver
    platform: linux/amd64
    hostname: sqlserver
    environment:
      MSSQL_SA_PASSWORD: "Str0ngP@$$w0rd"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - hoteldatabase:/var/opt/mssql/

volumes:
  hoteldatabase:
    